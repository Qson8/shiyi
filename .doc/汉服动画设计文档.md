# 拾衣坊（Shiyi）汉服主题动画设计文档

## 📐 设计理念

本动画系统结合汉服文化元素，创造出优雅、流畅、富有文化内涵的动画效果。设计灵感来源于：

- **汉服衣袖**：飘逸、柔和的转场效果
- **汉服展开**：层次分明、优雅的展开动画
- **汉服纹理**：流动、渐变的视觉体验
- **汉服飘带**：轻盈、动态的装饰效果
- **古代卷轴**：传统、典雅的页面切换

## 🎨 动画效果列表

### 1. 汉服衣袖飘动效果 (`createSleeveTransition`)

**用途**：页面转场动画

**特点**：
- 从右侧滑入（模拟衣袖飘入）
- 结合淡入和轻微缩放效果
- 使用柔和的曲线（`hanfuCurve`）

**使用示例**：
```dart
Navigator.push(
  context,
  HanfuAnimations.createSleeveTransition(
    YourPage(),
    duration: HanfuAnimations.normal,
  ),
);
```

### 2. 汉服展开效果 (`createExpandAnimation`)

**用途**：卡片进入动画

**特点**：
- 从中心展开
- 淡入效果
- 轻微上移（模拟汉服展开时的上升感）

**使用示例**：
```dart
AnimatedBuilder(
  animation: yourAnimationController,
  builder: (context, child) {
    return HanfuAnimations.createExpandAnimation(
      animation: yourAnimationController,
      child: YourCard(),
    );
  },
);
```

### 3. 汉服纹理流动效果 (`createFlowAnimation`)

**用途**：图标动画

**特点**：
- 渐变遮罩流动
- 模拟汉服纹理的流动感
- 可自定义流动颜色

**使用示例**：
```dart
HanfuAnimations.createFlowAnimation(
  controller: animationController,
  flowColor: AppTheme.primaryColor,
  child: Icon(Icons.checkroom_rounded),
);
```

### 4. 汉服卷轴展开效果 (`createScrollTransition`)

**用途**：页面切换动画

**特点**：
- 从下往上展开（模拟卷轴展开）
- 结合淡入效果
- 使用柔和的曲线

**使用示例**：
```dart
Navigator.push(
  context,
  HanfuAnimations.createScrollTransition(
    YourPage(),
    duration: HanfuAnimations.slow,
  ),
);
```

### 5. 汉服纹理渐变加载动画 (`createTextureLoadingAnimation`)

**用途**：加载指示器

**特点**：
- 渐变纹理逐渐显现
- 可自定义颜色
- 模拟汉服纹理的渐变效果

**使用示例**：
```dart
HanfuAnimations.createTextureLoadingAnimation(
  controller: animationController,
  primaryColor: AppTheme.primaryColor,
  child: YourLoadingWidget(),
);
```

### 6. 汉服图案呼吸效果 (`createBreathingAnimation`)

**用途**：图标或装饰元素

**特点**：
- 轻微缩放和透明度变化
- 模拟呼吸的节奏感
- 可自定义缩放和透明度范围

**使用示例**：
```dart
HanfuAnimations.createBreathingAnimation(
  controller: animationController,
  minScale: 0.95,
  maxScale: 1.0,
  child: Icon(Icons.auto_awesome_rounded),
);
```

### 7. 汉服飘带效果 (`createRibbonAnimation`)

**用途**：装饰性元素

**特点**：
- 飘动的偏移和旋转
- 模拟汉服飘带的动态效果
- 可自定义偏移量

**使用示例**：
```dart
HanfuAnimations.createRibbonAnimation(
  controller: animationController,
  maxOffset: 5.0,
  child: YourRibbonWidget(),
);
```

### 8. 汉服层次展开效果 (`createLayeredAnimation`)

**用途**：列表项进入动画

**特点**：
- 按索引延迟启动
- 创建层次感
- 结合缩放、淡入和上移

**使用示例**：
```dart
HanfuAnimations.createLayeredAnimation(
  animation: animationController,
  index: itemIndex,
  delayPerItem: Duration(milliseconds: 100),
  child: YourListItem(),
);
```

## 🧩 预构建组件

### AnimatedHanfuCard

带汉服动画效果的卡片组件，自动应用层次展开动画。

**属性**：
- `index`: 用于控制动画延迟
- `enableEntranceAnimation`: 是否启用进入动画

**使用示例**：
```dart
AnimatedHanfuCard(
  index: 0,
  padding: EdgeInsets.all(20),
  child: YourContent(),
)
```

### AnimatedHanfuIcon

带汉服动画效果的图标组件。

**属性**：
- `enableFlowAnimation`: 启用流动动画
- `enableBreathingAnimation`: 启用呼吸动画

**使用示例**：
```dart
AnimatedHanfuIcon(
  icon: Icons.checkroom_rounded,
  size: 28,
  enableFlowAnimation: true,
)
```

### HanfuLoadingIndicator

汉服主题的加载指示器。

**使用示例**：
```dart
HanfuLoadingIndicator(
  message: '加载中...',
  size: 50.0,
)
```

## ⚙️ 动画参数

### 时长常量

- `fast`: 200ms - 快速动画
- `normal`: 400ms - 标准动画
- `slow`: 600ms - 慢速动画
- `verySlow`: 1000ms - 很慢的动画

### 动画曲线

- `hanfuCurve`: `Curves.easeOutCubic` - 汉服风格的柔和曲线
- `gentleCurve`: `Curves.easeInOut` - 柔和的曲线
- `flowCurve`: `Curves.easeInOutSine` - 流动感的曲线

## 🎯 最佳实践

1. **页面转场**：使用 `createSleeveTransition` 或 `createScrollTransition`
2. **卡片列表**：使用 `AnimatedHanfuCard` 并设置不同的 `index`
3. **图标动画**：使用 `AnimatedHanfuIcon` 并选择合适的动画类型
4. **加载状态**：使用 `HanfuLoadingIndicator`
5. **装饰元素**：使用 `createRibbonAnimation` 或 `createBreathingAnimation`

## 📱 已应用位置

- ✅ 首页欢迎卡片：使用 `AnimatedHanfuCard` + `AnimatedHanfuIcon`（呼吸动画）
- ✅ 首页功能卡片：使用 `AnimatedHanfuCard` + `AnimatedHanfuIcon`（流动动画）
- ✅ 动画演示页面：展示所有动画效果

## 🔮 未来扩展

- [ ] 页面转场动画集成到 GoRouter
- [ ] 知识库列表项动画
- [ ] 衣橱列表项动画
- [ ] 3D模型加载动画
- [ ] 手势交互动画

## 📝 注意事项

1. 所有动画控制器需要在 `dispose` 时释放
2. 使用 `mounted` 检查确保在 widget 销毁后不更新状态
3. 动画时长应根据实际场景调整
4. 避免在低端设备上使用过多复杂动画

